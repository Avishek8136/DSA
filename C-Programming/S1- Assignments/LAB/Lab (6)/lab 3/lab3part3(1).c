#include<stdio.h>
int main()
{
	char cnt=0;
	for(;cnt++;printf("%d",cnt));
	printf("%d",cnt);
	return 0;
}
/*Output 1 Here the variable cnt is intialized and has data type next in
 * the for there is an increament value for cnt cnt++ cnt+1=0+1=1 ; 
 * printf("%d",cnt) is to print each value if any changes occur; atlast
 * it prints the last value.*/
